controller ansible_host=openstack-node ansible_user=centos

# This host should be one from the compute host group.
# Playbooks are not prepared to deploy tungsten fabric compute node separately.
tf_controller ansible_host=tf-node ansible_user=centos

[compute]
tf-node ansible_user=centos