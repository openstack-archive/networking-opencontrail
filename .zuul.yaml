- project:
    check:
      jobs:
        - contrail-nightly

- job:
    voting: false
    name: contrail-nightly
    parent: legacy-dsvm-base
    timeout: 3000
    required-projects:
      - openstack-infra/devstack-gate
    run: playbooks/contrail-nightly.yaml

